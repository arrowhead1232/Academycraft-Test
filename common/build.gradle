plugins {
    id 'multiloader-common'
    id 'net.neoforged.moddev.legacyforge'
}

legacyForge {
    mcpVersion = minecraft_version
    accessTransformers = [project(":forge").file("src/main/resources/META-INF/accesstransformer.cfg").path]
    parchment {
        minecraftVersion = parchment_minecraft
        mappingsVersion = parchment_version
    }
}

dependencies {
    compileOnly(group: "org.spongepowered", name: "mixin", version: "0.8.5")
    implementation('org.ow2.asm:asm:9.7.1')
    compileOnly("net.neoforged:bus:${bus_version}")
    api("org.jetbrains:annotations:26.0.2")
    implementation(group: 'com.google.code.findbugs', name: 'jsr305', version: '3.0.1')
    modCompileOnlyApi("mezz.jei:jei-${minecraft_version}-common-api:${jei_version}")
    modCompileOnly("mezz.jei:jei-${minecraft_version}-common:${jei_version}")
    modCompileOnly("dev.kosmx.player-anim:player-animation-lib:${player_anim}")
}

configurations {
    commonJava {
        canBeResolved = false
        canBeConsumed = true
    }
    commonResources {
        canBeResolved = false
        canBeConsumed = true
    }
}

artifacts {
    commonJava sourceSets.main.java.sourceDirectories.singleFile
    commonResources sourceSets.main.resources.sourceDirectories.singleFile
}